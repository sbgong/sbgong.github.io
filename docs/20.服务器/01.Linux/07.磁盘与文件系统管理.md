---
title: 磁盘与文件系统管理
date: 2022-04-30 15:18:17
permalink: /pages/35e63e/
categories:
  - 服务器
tags:
  - Linux
---

## Linux 文件系统

文件系统是操作系统中负责管理持久数据的子系统，说简单点，就是负责把用户的文件存到磁盘硬件中，因为即使计算机断电了，磁盘里的数据并不会丢失，所以可以持久化的保存文件

文件系统的基本数据单位是文件，它的目的是对磁盘上的文件进行组织管理，那组织的方式不同，就会形成不同的文件系统

### 文件系统特性

磁盘分区完毕后需要进行格式化（format），之后操作系统才能够使用这个文件系统。『为什么需要进行格式化磁盘？』因为每种操作系统所设定的文件属性/权限并不相同，为了存放这些文件所需的数据，因此就需要将分区槽进行格式化，已成为操作系统能够利用的文件系统格式

每种操作系统能够使用的文件系统并不相同，Windows98 以前的微软操作系统主要利用的文件系统是 FAT，Windows2000 以后的版本是 NTFS 文件系统。而 Linux 的正统文件系统是 Ext2。在默认情况下，Windows 系统是不会认识 Ext2 的

传统的磁盘与文件系统应用中，一个分区槽就只能够被格式化成一个文件系统。但由于新技术的应用，例如 LVM 与软件磁盘阵列，这些技术可以将一个分区槽格式化为多个文件系统（例如 LVM），也能够将多个分区槽合并成一个文件系统（LVM、RAID）。所以说，目前**可以称一个被挂载的数据为一个文件系统**

::: note 索引式文件系统
操作系统的文件数据除了文件实际内容外，通常含有非常多的属性，文件系统通常会将这两部分的数据分别存放在不同的区块，权限与属性放置到 inode 中，实际数据则放到 data block 区块中。另外还有一个超级区块（superblock）会记录整个文件系统的整体信息，包括 inode 与 block 的总量、使用量、剩余量等（每个 inode 与 block 都有编号）

- `superblock`：记录此文件系统的整体信息，包括 inode/block 的总量、使用量、剩余量以及文件系统的格式与相关信息等
- `inode`：记录文件的属性，一个文件占用一个 inode，同时记录此文件的数据所在的 block 号码
- `block`：实际记录文件的内容，若文件太大时，会占用多个 block

由于每个 inode 和 block 都有编号，而每个文件都会占用一个 inode，inode 内有文件数据位置的 block 号码，因此如果找到文件的 inode 的话，那么自然就会知道这个文件所放置数据的 block 号码，当然也就能够读出该文件的实际数据了。这种数据读取的方法被称为**索引式文件系统**
:::

而 FAT 格式的文件系统(例如随身碟)并没有 inode 的存在，所以 FAT 无法将一个文件的所有 block 在一开始就读取出来。FAT 的每个 block 号码都记录在前一个 block 当中，它需要一个一个的将 block 读出后，才会知道下一个 block 在何处。『碎片整理』就是因为文件写入的 block 太过于分散，此时文件读取的效能就会变得很差，这个时候可以通过碎片整理将同一个文件所属的 blocks 汇集在一起，这样的数据读取会比较容易。FAT 文件系统需要经常的碎片整理一下，而 Ext2 是索引式文件系统，不太需要碎片整理

### EXT2 文件系统

![ext2文件系统](https://cdn.jsdelivr.net/gh/sbgong/static/blog/server/linux/202205021443326.png)

::: note Linux 文件系统 Ext2 文件系统

- inode 的内容在记录文件的权限与相关属性，而 block 区块则是在记录文件的实际内容。而且文件系统在格式化之后就将 inode 和 block 规划好了，除非重新格式化（或者利用 resize2fs 等指令变更文件系统大小），否则 inode 和 block 固定之后就不再变动
- 但是如果文件系统高达数百 GB 时，将所有的 inode 和 block 统统放在一起不太合理，因为 inode 和 block 的数量太庞大，不容易管理。为此，Ext2 文件系统在格式化的时候基本上是区分为多个区块群组（block group），每个区块群组都有独立的 inode/block/superblock 系统
- 而在整体规划磁盘的时候，文件系统最前面有一个启动扇区（boot sector），这个启动扇区可以安装开机管理程序，如此一来就能够将不同的开机管理程序安装到个别的文件系统最前面，而不用覆盖整个磁盘的 MBR，这样也才能够制作出多重引导的环境

:::

每一个区块群组有六个主要内容：

:::: tabs cache-lifetime="5" :options="{ useUrlFragment: false }"
::: tab data-block
这是用来放置文件内容数据的地方，在 Ext2 文件系统中所支持的 block 大小有 1K/2K/4K 三种。在格式化的时候 block 大小就固定了，且每个 block 都有编号，方便 inode 的记录。注意：由于 block 大小的差异，会导致该文件系统能够支持的最大磁盘容量与最大单一文件容量并不相同。因此 block 大小而产生的 ext2 文件系统限制如下：

|     block 大小     | 1KB  |  2KB  | 4KB  |
| :----------------: | :--: | :---: | :--: |
|  最大单一文件限制  | 16GB | 256GB | 2TB  |
| 最大文件系统总容量 | 2TB  |  8TB  | 16TB |

- 原则上，block 的大小与数量在格式化完就不能够再改变了（除非重新格式化）
- 每个 block 内最多只能够放置一个文件的数据
- 如果文件大于 block 的大小，则一个文件会占用多个 block 数量；如果文件小于 block，则该 block 的剩余容量就不能够再被使用了（磁盘空间会浪费）

:::
::: tab inode-table
系统读取文件时需要先找到 inode，并分析它记录的权限与用户是否符合，若符合才能够开始实际读取 block 的内容。inode 记录的文件数据至少有底下这些：

- 文件的存取权限（read/write/execute）
- 文件的拥有者和群组
- 文件的容量
- 文件建立或状态改变的时间（ctime）
- 最近一次的读取时间（atime）
- 最近一次修改文件内容的时间（mtime）
- 定义文件的特殊权限，如 SUID
- 该文件真正内容的指向

inode 要记录的数据非常多，但 inode 的大小仅仅只有 128bytes，而 inode 记录一个 block 号码要花掉 4bytes，假设一个文件有 400M 且每个 block 为 4KB，那么至少也在 10 万条 block 号码的记录，inode 哪能记录这么多信息呢，inode 空间也不够啊；因此系统将 inode 记录 block 号码的区域定义为 12 个直接、一个间接、一个双间接与一个三间接记录；

- 直接就是直接指向 block 号码
- 间接就是拿一个 block 来当作记录 block 号码的记录区；如果文件太大时，就会使用间接的 block 来记录编号

![block](https://cdn.jsdelivr.net/gh/sbgong/static/blog/server/linux/202205021444521.png)

假设每个 block 的大小为 1KB:

- 12 个直接：12 x 1=12K;
- 一个间接：256 x 1K=256K; 256=1KB(一个 block) / 4byte(记录一个 block 索引需要花费的大小)
- 一个双间接：256 x 256 x 1KB=256^2K
- 一个三间接：256 x 256 x 256 x 1KB=256^3K

总共加起来为：16GB；所以当文件系统 block 格式化为 1K 大小时，能够容纳的最大文件为 16GB
:::
::: tab super-block
超级区块是记录整个文件系统相关信息的地方，一般来说，superblock 的大小为 1024bytes。一个文件系统应该只有一个 superblock，如果其他组中也有 superblock，那主要是作为第一个 block group 内 superblock 的备份

- block 和 inode 的总量
- 未使用与已使用的 inode/block 数量
- block 与 inode 的大小（block 为 1/2/4K，inode 为 128bytes 或 256bytes）
- 文件系统的挂载时间、最近一次写入数据的时间、最近一次检验磁盘的时间（fsck）等文件系统的相关信息
- 一个`valid bit`数值，若此文件系统已被挂载，则该值为 0；若未被挂载，则该值为 1

:::
::: tab file-system-description
**文件系统描述说明**：这个区段可以描述每个 block group 的开始与结束的 block 号码，以及说明每个区段分别介于哪一个 block 号码之间
:::
::: tab block-bitmap
**区块对照表**：从这里可以知道哪些 block 是空的，因此我们的系统可以很快的找到使用的空间来存储文件；同样的，如果你要删除某些文件时，那些文件原本占用的 block 号码就得要释放出来，此时在 block bitmap 当中相对应到该 block 号码的标志就得修改为『未使用中』
:::
::: tab inode-bitmap
**inode 对照表**：这是记录使用与未使用的 inode 号码
:::
::::

### xfs 文件系统

xfs 是一个日志式文件系统，在资料的分布上，主要规划为三个部分：数据区（data section）、文件系统活动登录区（log section）以及实时运作区（realtime section）

:::: tabs cache-lifetime="5" :options="{ useUrlFragment: false }"
::: tab 数据区
数据区和 ext 文件系统一样，包括 inode/data block/superblock 等数据，都放置在这个区块。这个数据区和 ext 文件系统的 block group 类似，也是分为多个群组（allocation groups）来分别放置文件系统所需要的数据。每个群组都包含了

- 整个文件系统的 superblock
- 剩余空间的管理机制
- inode 的分配与追踪

此外 inode 与 block 都是系统用到时，才动态配置产生的，所以格式化动作超级快。另外与 ext 文件系统不同的是，xfs 的 block 与 inode 有多种不同的容量可供设定，block 容量由 512 字节-64KB 调配。不过，Linux 环境下，由于内存控制的关系，最高可使用的 block 大小为 4K，至于 inode 容量可由 256bytes 到 2M 这么大，还是保留 256 字节的默认值就够用了
:::
::: tab 文件系统活动登录区
这个区域主要被用来记录文件系统的变化，有点像是日志区。文件的变化会在这里记录下来，直到该变化完整的写入到数据区后，该笔记录才会被终结。如果文件系统因为某些缘故（例如停电）而损毁时，系统会拿这个登录区块来进行校验，看看系统挂掉之前，文件系统正在运作些什么动作，借以快速的修复文件系统
:::
::: tab 实时运作区
当有文件要被建立时，xfs 会在这个区段里面找一个到数个 extend 区块，将文件放置在这个区块内，等到分配完毕后，再写入到 data section 的 inode 与 block 去。这个 extend 区块的大小得要再格式化的时候就先指定，最小值是 4K 最大可到 1G。这个 extend 最好不要乱动，因为可能会影响到实体磁盘的效能
:::
::::

## 文件系统的简单操作：df/du/ln

- `df`：用于显示 Linux 系统中各文件系统的硬盘使用情况
- `du`：统计目录或文件所占磁盘空间大小
- `ln`：建立硬链接或者软链接

:::: tabs cache-lifetime="5" :options="{ useUrlFragment: false }"
::: tab "df 指令语法"
**指令语法**：`df [选项] [目录或文件名]`
| 选项参数 | 说明 |
| -------- | --------------------------------------------------- |
| `-a` | 列出所有的文件系统，包括系统特有的 /proc 等文件系统 |
| `-k` | 以 KB 为单位显示容量，默认以 KB 为单位 |
| `-m` | 以 MB 为单位显示容量 |
| `-h` | 使用人们习惯的 KB、MB 或 GB 等单位自行显示容量 |
| `-H` | 以 M=1000K 取代 M=1024K 的进位方式 |
| `-T` | 显示该分区的文件系统名称 |
| `-i` | 不用硬盘容量显示，而是以含有 inode 的数量来显示 |

:::
::: tab "du 指令语法"
**指令语法**：`du [选项] [目录或文件名]`
| 选项参数 | 说明 |
| -------- | -------------------------------------------------------- |
| `-a` | 显示每个子文件的磁盘占用量。默认只统计子目录的磁盘占用量 |
| `-h` | 使用习惯单位显示磁盘占用量，如 KB、MB 或 GB 等 |
| `-s` | 统计总磁盘占用量，而不列出子目录和子文件的磁盘占用量 |
| `-S` | 不包括子目录下的统计，与 -s 有点差别 |
| `-k` | 以 KB 为单位显示容量 |
| `-m` | 以 MB 为单位显示容量 |
:::
::: tab "ln 指令语法"
**指令语法**：`ln [选项] 源文件 目标文件`
| 选项参数 | 说明 |
| -------- | ---------------------------------------------------- |
| `-s` | 加上该选项是软链接，不加该选项则是硬链接 |
| `-f` | 如果目标文件存在，就主动的将目标文件直接移除后再建立 |

**注意**：硬链接不能跨文件系统，也不能链接目录
:::
::: tab "df 代码案例"

```shell
df
df -h       # 将容量结果以易读的容量格式i显示出来
df -aT      # 将系统内的所有特殊文件格式及名称都列出来
df -h /etc  # 将/etc底下的可用的磁盘容量以易读的容量格式显示
df -ih      # 将目前各个分区当中可用的inode数量列出
```

本例中，由 df 命令显示出的各列信息的含义分别是

- `Filesystem`：表示该文件系统位于哪个分区，因此该列显示的是设备名称
- `1K-blocks`：此列表示文件系统的总大小，默认以 KB 为单位
- `Used`：表示用掉的硬盘空间大小
- `Available`：表示剩余的硬盘空间大小
- `Use%`：硬盘空间使用率。如果使用率高达 90% 以上，就需要额外注意，因为容量不足，会严重影响系统的正常运行
- `Mounted on`：文件系统的挂载点，也就是硬盘挂载的目录位置

:::
::: tab "du 代码案例"

```shell
du        # 列出目前目录下的所有文件容量（每个目录都会列出来）
du -a     # 将文件的容量也列出来
du -sm /* # 检查根目录底下每个目录所占用的容量
```

:::
::::

## 磁盘的分区/格式化/检验与挂载

### 观察磁盘分区：lsblk/blkid/parted

:::: tabs cache-lifetime="5" :options="{ useUrlFragment: false }"
::: tab lsblk

> `lsblk`：列出系统上的所有磁盘列表

**指令语法**：`lsblk [选项] [device]`
| 选项参数 | 说明 |
| -------- | ---------------------------------------------------------------- |
| `-d` | 仅列出磁盘本身，并不会列出该磁盘的分区数据 |
| `-f` | 同时列出磁盘内的文件系统名称 |
| `-i` | 使用 ASCII 的线段输出，不要使用复杂的编码（在某些情况下很有用） |
| `-m` | 同时输出该装置在/dev 底下的权限数据（rwx 的数据） |
| `-p` | 列出该装置的完整文件名，而不是仅列出最后的名字 |
| `-t` | 列出该磁盘装置的详细数据，包括磁盘队列机制，预读写的数据量大小等 |
:::
::: tab blkid

> `blkid` 可以列出文件系统名称与设备的 UUID 等数据；如果分区没有挂载目录，则不会显示

UUID：全局单一标识符，Linux 会将系统内所有的装置都给予一个独一无二的标识符，这个标识符就可以拿来作为挂载或者是使用这个装置/文件系统之用了）

使用的时候，直接输出 `blkid` 就可以了
:::
::: tab parted

> `parted`：列出磁盘的分区表类型与分区信息

**指令语法**：`parted 设备名称 print`

```shell
parted /dev/vda print
```

:::
::: tab "lsblk 代码案例"

```shell
[zhanghy@zhanghy ~]$ lsblk    # 列出本系统下的所有磁盘与磁盘内的分区信息
NAME                  MAJ:MIN RM  SIZE RO TYPE MOUNTPOINT
sda                     8:0    0  100G  0 disk
├─sda1                  8:1    0    1G  0 part /boot
├─sda2                  8:2    0    1G  0 part [SWAP]
└─sda3                  8:3    0   98G  0 part
  └─centos_zhang-root 253:0    0   98G  0 lvm  /
sr0                    11:0    1  4.4G  0 rom  /run/media/zhanghy/CentOS 7 x86_64
```

- `NAME`：就是装置的文件名，会省略/dev 等前导目录
- `MAJ:MIN`：其实核心认识的装置都是通过这两个代码来熟悉的，分别是主要:次要装置代码
- `RM`：是否为可卸载装置，如光盘、USB
- `SIZE`：容量
- `RO`：是否为只读装置
- `TYPE`：是磁盘、分区槽还是只读存储器
- `MOUNTPOINT`：挂载点

:::
::::

### 磁盘分区：gdisk/fdisk/partprobe/parted

:::: tabs cache-lifetime="5" :options="{ useUrlFragment: false }"
::: tab "gdisk（GPT 分区表）"
**指令语法**：`gdisk 装置名称`；并通过使用 `?` 作为指令提示数据进行界面操作
| 交互字母 | 说明 |
| -------- | ------------ |
| `d` | 删除一个分区 |
| `n` | 新建分区 |
| `P` | 打印分区表 |
| `q` | 不保存退出 |
| `w` | 保存退出 |

![分区表信息](https://cdn.jsdelivr.net/gh/sbgong/static/blog/server/linux/202205042241976.png)
:::
::: tab "fdisk（MBR 分区表）"
**指令语法**：`fdisk 装置名称`；并通过使用 `m` 作为指令提示数据进行界面交互
fdisk 和 gdisk 非常相似，其他参考 gdisk 就行
:::
::: tab "partprobe 更新分区表"

> `partprobe`：用 gdisk/fdisk/parted 新增/删除一个分区后，使用 lsblk 或者 cat /proc/partitions 查看磁盘信息的时候，发现磁盘的新增/删除的信息并没有更新。因此这块磁盘正在使用，系统无法加载到新的分区表中，这个时候就可以使用 partprobe 命令更新

**指令语法**：`partprobe [-s]`；`-s` 参数：会在屏幕上出现信息，不加则不显示信息
:::
::: tab "parted 通用分区指令"

> 通用的分区指令：利用 GNU 的 parted 进行分区行为

**指令语法**：`parted [装置] [指令 [参数]]`

| 指令功能                                                                     | 说明             |
| ---------------------------------------------------------------------------- | ---------------- |
| `mkpart [primary/logical/Extended] [文件系统格式] 分区开始内存 分区结束内存` | 新增分区         |
| `print`                                                                      | 打印磁盘分区信息 |
| `rm [分区号码]`                                                              | 删除分区         |

![print打印信息](https://cdn.jsdelivr.net/gh/sbgong/static/blog/server/linux/202205061403710.png)
:::
::::

### 磁盘格式化：mkfs/mkfs.xfs/mkfs.ext4

格式化其实就是创建文件系统。命令就是`mkfs`（make fileststem），这个命令只是个综合命令。它会调用正确的文件系统格式化工具

mkfs 指令语法：`mkfs [-t 文件系统格式] 分区设备文件名` 或 `mkfs.文件系统名`

- mkfs[tab][tab]：按下两下 tab 键查看系统支持哪些文件系统的格式化功能
- `mkfs [-t 文件系统格式名]` 等同于 `mkfs.文件系统格式名`（即 `mkfs -t xfs` 与 `mkfs.xfs` 相同）
  - 用于指定格式化的文件系统，如 xfs、ext4 等

:::: tabs cache-lifetime="5" :options="{ useUrlFragment: false }"
::: tab mkfs.xfs
**指令语法**：`mkfs.xfs [-b 参数] [-d 参数] [-i 参数] [-l 参数] [-L 参数] [-f] [-r 参数] 设备名称`
| 选项参数 | 说明 |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `-b` | 后面接的是 block 容量，范围是 512B-64K。不过 Linux 最大为 4K |
| `-d` | 后面接的是 data section（数据区）的相关参数值：<br>（1）agcount=数值 ：设置需要几个存储群组的意思（AG），通常与 CPU 有关<br>（2）agsize=数值 ：每个 AG 设置为多少容量的意思，通常 agcount/agsize 只选一个设置<br>（3）file ：指的是格式化的设备是个文件而不是个装置的意思（例如虚拟磁盘）<br>（4）size=数值 ：data section 的容量，即你可以不将全部的装置容量用完的意思<br>（5）su=数值 ：当有 RAID（磁盘列阵）时，代表 stripe 的数值大小，与下面的 sw 搭配使用<br>（6）sw=数值 ：当有 RAID 时，用于保存数据的磁盘数量（需扣除备份盘与备用盘）<br>（7）sunit=数值 ：与 su 意思相同，不过单位使用的是 sector<br>（8）swidth=数值 ：就是 su\*sw 的数值，不过单位使用的是 sector |
| `-f` | 如果设备内已经有了文件系统，则需要使用-f 强制格式化 |
| `-i` | 与 inode 有较相关的设置：<br>（1）size=数值 ：最小的是 256B，最大是 2K。一般使用 256B 就足够了<br>（2）internal=[0/1] ：log 设备是否为内置。默认 1 为内置，如果使用外部设备，使用下面设置<br>（3）logdev=设备名 ：log 设备为后面接的那个设备上面的意思，需设置 internal=0 才可以<br>（4）size=数值 ：指定这块登录区的容量，通常最小得要 512 个区块，大约 2M 以上才行 |
| `-L` | 后面接这个文件系统的标头名称 Label name |
| `-r` | 指定 realtime section（实时运行区）的相关设置值：<br>（1）extsize=数值 ：就是那个重要的 extent 区块数值，一般不需设置。但有 RAID 时，最好设置与 swidth 的数值相同较佳。最小为 4K，最大为 1G |

:::
::: tab mkfs.ext4
**指令语法**：`mkfs.ext4 [-b size] [-L label] 装置名称`
| 选项参数 | 说明 |
| -------- | ------------------------------------ |
| `-b` | 后面设置 block 的大小，有 1K、2K、4K |
| `-L` | 后面接这个设备的标头名称 Label name |

- ext4 的默认值已经相当合适我们系统使用，大部分的默认值写入至/etc/mke2fs.conf 这个文件中
- 因此我们无需额外指定 inode 的容量，系统都帮我们做好了默认值，只需要知道 uuid 即可

:::
::: tab "mkfs 代码案例"

```shell
mkfs.xfs /dev/vda4  # 使用默认值直接格式化，很快便能格式化完毕，如果有额外想要处理的项目，才需要加上一堆设定值
mkfs.xfs -f -d agcount=2,su=256K,sw=7 -r extsize=1792K /dev/vda4
mkfs.ext4 /dev/sda2
```

:::
::::

### 文件系统检验：xfs_repair/fsck

::: tip 文件系统检验的产生背景
系统运行时，如果硬件或者电源等出问题，或者文件系统运行时，磁盘与内存数据异步的状态发生，系统会出错。此时就需要文件系统的校验，不同的文件系统命令不一样，下面我们主要针对 xfs 于 ext4 文件系统进行讲解
:::

:::: tabs cache-lifetime="5" :options="{ useUrlFragment: false }"
::: tab "xfs_repair"
**xfs_repair 处理 XFS 档案系统**，主要对 xfs 文件系统进行检测或者修复

**指令语法**：`xfs_repair [选项] 装置名称`
| 选项参数 | 说明 |
| -------- | ----------------------------------------------------------------------------- |
| `-f` | 后面的设备其实是个文件而不是设备时使用 |
| `-n` | 单纯检查并不修改文件系统的任何数据 |
| `-d` | 通常用在单人维护模式下，针对根目录（/）进行检查与修复（很危险，不要随便使用） |
:::
::: tab "fsck.ext4"
**fsck.ext4 处理 EXT4 档案系统**，主要对 ext4 文件系统进行检测或者修复

- fsck 是一个综合指令，如果针对 ext4 的话，建议直接使用 `fsck.ext4`来检测比较妥当

**指令语法**：`fsck.ext4 [选项] [-b 参数] 设备名称`
| 选项参数 | 说明 |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `-p` | 当文件系统在修复时，如果有需要回复 y 的操作，自动回复 |
| `-f` | 强制详细检查。一般来说 fsck 没有发现任何 unclean 的标识，不会主动进入详细检查的。如果加上这个参数，fsck 就会详细检查 |
| `-D` | 针对文件系统下的目录进行最佳化配置 |
| `-b` | 后面接 superblock 的位置，这个选项一般用不到。如果你的超级区块受损时，就可以使用这个参数利用文件系统的备份超级区块来尝试修复。一般来说，超级区块备份在：1K 区块在 8193、2K 区块在 16384、4K 区块在 32768 |
:::
::::

::: danger

- 无论是 `xfs_repair` 还是 `fsck.ext4` 这都是用来检查与修正文件系统错误的指令。通常只有身为 root 且你的文件系统有问题的时候才使用这个指令，否则在正常情况下使用此命令，可能会造成对系统的危害
- 修复文件系统时，可能会造成文件系统的改变，所以执行命令时，被检测的硬盘分区不能被挂载，若挂载则不能顺利的修复文件系统，需要卸载后再处理。当然，没有挂载的文件系统可以处理
  :::

### 文件系统挂载：mount/umount

::: tip 要进行挂载之前，先确定几件事：

- 单一文件系统不应该被重复挂载在不同的挂载点（目录）中
- 单一目录不应该重复挂载多个文件系统
- 要作为挂载点的目录，理论上应该都是空目录才行

如果要用来挂载的目录里面并不是空的，那么挂载了文件系统之后，原目录下的东西就会暂时消失，并不是被覆盖掉了，而是暂时的隐藏起来了。等到新分区槽被卸载之后，原来的内容就会再次跑出来
:::

系统自动挂载的信息记录在 `/etc/fstab` 文件中，只有记录到这个文件中的磁盘在系统启动时才会自动挂载。不在这个文件中记录的磁盘挂载了关机后就自动卸载，开机后又需要自己手动挂载

:::: tabs cache-lifetime="5" :options="{ useUrlFragment: false }"
::: tab mount
**指令语法**：

- `mount -a`
- `mount [-l]`
- `mount [-t 文件系统] label='' 挂载点` 使用磁盘的 Label name 来挂载磁盘
- `mount [-t 文件系统] UUID='' 挂载点` 使用磁盘的 UUID 来挂载磁盘
- `mount [-t 文件系统] 装置文件名 挂载点` 使用磁盘的设备文件名来挂载磁盘

| 选项参数 | 说明                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `-a`     | 依照配置文件 `/etc/fstab` 的数据将文件内未挂载的磁盘都挂载上来                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `-l`     | 单纯的输入 mount 会显示目前挂载的信息，加上-l 之后可增列 Label 名称                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `-t`     | 可以加上文件系统的种类名来指定预挂载的类型。Linux 常见支持的类型有：xfs、ext3、ext4、reiserfs、vfat、iso9660（光盘格式）、nfs、cifs、smbfs（后三种是网络文件系统类型）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `-n`     | 在默认的情况下，系统会将实际挂载的情况即时写入 `/etc/mtab` 中，以利于其他程序的运行。但是在某些情况下（例如单人维护模式），为了避免问题会刻意不写入，此时就需要加上-n 选项                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `-o`     | 后面可以接一些挂载时额外加上的参数。比如：账号、密码、读写权限等：<br>（1）async,sync ：此文件系统是否使用同步写入的内存机制。默认为 async<br>（2）atime,noatime ：文件系统是否会修改文件的读取时间。为了性能，某些时刻会使用 noatime<br>（3）ro,rw ：文件系统是只读(ro)还是可读写(rw)<br>（4）auto,noauto ：文件系统是否以 mount -a 自动挂载<br>（5）dev,nodev ：文件系统是否可建立设备文件<br>（6）suid,nosuid ：文件系统是否含有 suid/sgid 的文件格式<br>（7）exec,noexec ：文件系统是否拥有可执行的二进制文件<br>（8）user,nouser ：是否允许文件系统让任何使用者执行 mount。一般 mount 仅有 root 可以进行<br>（9）defaults ：默认值为：rw、suid、dev、exec、auto、nouser、async<br>（10）remount ：重新挂载。这在系统出错，或重新更新参数时很有用<br>（11）codepage=数值 ：挂载 USB 移动磁盘时，指定挂载的文件系统所使用的语系。中文为 950<br>（12）iocharset=编码名 ：挂载 USB 移动磁盘时，指定移动磁盘的编码。一般有：utf8、Big5<br>（13）loop ：Linux 下的文件制作 loop 设备挂载时时会用到 |

:::
::: tab umount
**指令语法**：`umount [选项] 装置文件名或挂载点`
| 选项参数 | 说明 |
| -------- | ------------------------------------------------------- |
| `-f` | 强制卸载，可用在类似网络文件系统（NFS）无法读取的情况下 |
| `-l` | 立刻卸载文件系统，比-f 还强 |
| `-n` | 不更新/etc/mtab 情况下卸载 |

- 文件系统卸载的方式，最后可以是设备文件名也可以是挂载点(建议)。但是如果你的挂载点被另一个挂载点所挂载，则必须使用挂载点卸载
- 如果你当前的路径在你的挂载点内，则你需要退出到当前挂载点路径外才可以卸载

:::
::::

::: note CentOS7 可以自动分析最恰当的文件系统来尝试挂载你需要的装置
我们可以不加上 `-t` 这个选项，系统会自动分析最恰当的文件系统来尝试挂载你需要的设备。那么系统是如何找出文件系统类型的呢？

- 由于文件系统几乎都有 superblock，我们的 Linux 可以通过分析 superblock 搭配 Linux 自己的驱动程序去测试挂载，如果测试成功，就立刻自动使用该类型的文件系统挂在起来
- 系统只会指定某些部分的类型的文件系统进行上述的挂载测试（主要参考下面这两个文件）
  - `/etc/filesystems`：系统指定的测试挂载文件系统类型的优先级
  - `/proc/filesystems`：Linux 系统已经加载的文件系统类型
- Linux 支持的文件系统的驱动程序都写在如下的目录中
  - `/lib/modules/$(uname -r)/kernel/fs/`
  - 例如 ext4 的驱动程序就写在：`/lib/modules/$(uname -r)/kernel/fs//ext4/` 这个目录下

:::

### 磁盘/文件系统参数修订：mknod/xfs_admin/tune2fs

某些时刻，我们可能需要修改目前文件系统的一些相关信息。例如要修改 Label name 或者 journal 的参数，或者其他磁盘/文件系统运行时的相关参数（例如 DMA 启动与否），这个时候就需要下面的一些命令

:::: tabs cache-lifetime="5" :options="{ useUrlFragment: false }"
::: tab mknod
**指令语法**：`mknod 装置文件名 [bcp] [Major] [Minor]` 用于创建一个设备文件，即特殊文件
| 选项参数 | 说明 |
| -------- | ----------------------------------------- |
| `-b` | 建立一个外接存储设备文件。例如磁盘等 |
| `-c` | 建立一个外接输入设备文件。例如鼠标/键盘等 |
| `-p` | 建立一个 FIFO 文件 |
| `Major` | 建立设备时，赋予的主要设备代码 |
| `Minor` | 建立设备时，赋予的次要设备代码 |
:::
::: tab 设备的主要与次要代码
**概念**：在 Linux 上面所有的设备都以文件来表示，那么这个文件是如何来表示这个设备的呢？就是通过 major 与 minor 这两个数值来表示。Linux 内核支持的设备数据也是通过这两个数值决定的

**注意**：Linux 内核 2.6 版本后，硬件文件名已经都可以被系统自动地实时产生，不需要我们手动建立设备文件。不过某些情况下还是需要手动建立设备文件。这就需要 `mknod` 指令来操作了

相关磁盘文件名与对应的主要、次要代码如下：
| 磁盘文件名 | major | minor |
| ------------ | ----- | ----- |
| `/dev/sda` | 8 | 0-15 |
| `/dev/sdb` | 8 | 16-31 |
| `/dev/loop0` | 7 | 0 |
| `/dev/loop1` | 7 | 1 |
:::
::: tab xfs_admin
`xfs_admin` 查看/修改 xfs 文件系统设备的 UUID 与 Lable name

**指令语法**：`xfs_admin [-lu] [-L Label] [-U uuid] 装置文件名`
| 选项参数 | 说明 |
| -------- | ------------------------- |
| `-l` | 列出这个设备的 label name |
| `-u` | 列出这个设备的 UUID |
| `-L` | 设置这个设备的 label name |
| `-U` | 设置这个设备的 UUID |
:::
::: tab tune2fs
`tune2fs` 查看/修改 ext4 文件系统设备的 UUID 与 Lable name
**指令语法**：`tune2fs [选项] [-L 参数] [-U 参数] 装置文件名`
| 选项参数 | 说明 |
| -------- | ----------------------------------------------- |
| `-l` | 类似于 dumpe2fs -h 的功能。查看超级区块内的数据 |
| `-L` | 设置这个设备的 label name |
| `-U` | 设置这个设备的 UUID |
:::
::::

## 设置开机挂载

### 修改 /etc/fstab

手动处理挂载是很不人性的，我们可以设置让系统在每次启动的时候就自动进行挂载，就不用手动再去修改

::: danger 前提：系统挂载的一些限制

- 根目录是必须要挂载的，并且一定要优先于其他挂载点被挂载进来
- 其他挂载点必须为已建立的目录，可任意指定，但一定要遵守系统目录架构原则（FHS）
- 所有挂载点，在同一时间内，只能挂载一次
- 所有硬盘分区在同一时间内，只能挂载一次
- 如若进行卸载，必须先将工作目录移到挂载点（及其子目录）下

:::

设置开机挂载的方法：在 `/etc/fstab` 文件内去修改，文件内容如下：

![/etc/fstab文件内容](https://cdn.jsdelivr.net/gh/sbgong/static/blog/server/linux/202205060121346.png)

- 第一列：磁盘装置文件名/UUID/Label name
- 第二列：挂载点——挂载点一定是个目录。（注意：如果是内存交换分区的话，因为内存交换分区无挂载点，此处填写 swap）
- 第三列：磁盘分区槽的文件系统——在手动挂载时可以让系统自动检测挂载。但是在这个文件当中，我们必须手动写入文件系统才行，例如：xfs、ext4、vfat、reiserfs、nfs 等
- 第四列：文件系统参数——就是 mount 指令中的特殊文件系统参数，那些特殊参数就是卸载这个字段中
- 第五列：能否被 dump 备份指令作用——dump 是一个用来做为备份的命令，不过现在有太多的备份方案，这个地方直接输入 0 就好了
- 第六列：是否以 fsck 检验扇区
  - 早期系统启动的过程中，会有一段时间去校验本机的文件系统是否完整(clean)，主要通过 fsck 去完成
  - 不过 xfs 文件系统会自己进行校验，所以 xfs 文件系统不需要进行这个操作，设置为 0

::: warning

- `/etc/fstab` 是系统启动时的配置文件，实际文件系统的挂载时记录到 `/etc/mtab` 与 `/proc/mounts` 这两个文件中（每次我们在修改 `/etc/fstab` 文件的挂载信息时，同时也会修改这两个文件）
- 承上，如果我们在 `/etc/fstab` 输入的数据错误，导致无法磁盘顺利启动，而进入单人维护模式，这时候根目录就变为只读的状态，当然此时也无法修改 `/etc/mtab` 与 `/proc/mounts` 这两个文件。此时就可以这一行命令 `mount -n -o remount,rw / `
  :::

### 使用 loop 设备挂载 CD/DVD 镜像文件

一般来说，我们从一个网站下载了一个 Linux 或其他所需的镜像文件（.iso）后，该镜像文件一般需要刻录成 CD/DVD 光盘才能够被系统使用。但是有一种方法，可以不需要将其刻录成为光盘就能够直接使用该镜像中的文件数据。方法就是讲镜像文件制作成一个 loop 设备来进行挂载

**方法**：使用 mount 命令，将镜像文件（.iso）制作成一个 loop 设备，并同时挂载到一个目录下。此时，该挂载目录内的文件数据就是镜像文件内的数据，当然我们也可以对这些数据进行查看与修改

**指令语法**：`mount -o loop 镜像文件目录 挂载目录`

## 内存交换空间：swap

- **概念**：存在硬盘中的一个分区，用来暂时存放内存中的数据
- **功能**：CPU 读取的数据都是来自于内存，如果内存不足的时候，为了让后续的程序可以顺利运行，就会将暂时不使用的程序与数据挪到内存交换分区中，这样内存就会空出来给需要的程序加载使用
- **特点**
  - 如果硬件的设备资源足够的话，一般用不到内存交换分区。如果物理内存不足时，会使用到内存交换分区。所以早期安装 Linux 的时候，会使用到两个硬盘分区，一个是根目录，一个是内存交换分区
  - 一般来讲，swap 分区容量应大于物理内存大小，建议是内存的两倍，但不超过 2GB
  - 内存交换分区默认处于停用状态，可以使用 `swapon` 命令启用或者写入到 `/etc/fstab` 配置文件中进行自动启用

::: tip 何时使用到内存交换分区？

- 目前主机的内存一般都在 4GB 以上，一般使用不到内存交换分区。但是服务器/工作站等需要，因为服务器/工作站有时可能会爆发出大量的网络请求，此时就需要内存交换分区来缓冲一下内存的使用量
- 如果你的主机支持电源管理模式，也就是说你的 Linux 主机可以进入休眠模式，那么，运行当中的程序状态会被记录到内存交换区，以作为唤醒主机的状态依据
- 另外，某些程序在运行时，可能会利用内存交换分区的特性来存放一些数据

综上所述，内存交换分区还是需要建立的，但是不需要建立太大
:::

:::: tabs cache-lifetime="5" :options="{ useUrlFragment: false }"
::: tab mkswap
**mkswap 指令：格式化 swap 文件系统**
指令语法：` mkswap [options] 装置名称 [size]`
:::
::: tab swapon

> swapon 指令：启动交换分区、观察交换分区信息等（与系统格式化 mount 命令很相似）

**指令语法**：`swapon [options] [specialfile...]`

- 启用交换分区：`swapon 设备文件名`
- 查看当前系统处于使用状态的交换分区信息：`swapon -s`
- 根据 `/etc/fstab` 配置文件的内容，将未自动启动的交换分区进行启用：`swapon -a`

:::
::: tab swapoff

> `swapoff` 与 swapon 命令相反，此命令是将内存交换分区关闭使用

**指令语法**：`swapoff [-va] [specialfile...]`

:::
::::

内存交换分区的创建，有两种方式可以创建

:::: tabs cache-lifetime="5" :options="{ useUrlFragment: false }"
::: tab "第一种：使用物理内存创建内存交换分区"

- 第一步：划分一个新的分区。使用 gdisk/fdisk 命令在磁盘上划分出一个分区给系统作为内存交换分区
- 第二步：格式化。使用 mkswap 对分区进行格式化
- 第三步：启动 swap 设备。使用 swapon 命令
- 第四步（自选）：设置开机自动启动。将信息写入/etc/fstab 文件中
- 第五步（自选）：观察状态。使用 free 与 swapon -s 命令观察内存的使用量

```shell
gdisk /dev/sda    # 划分出一个新的分区
n
w
y
partprobe

mkswap /dev/sda5    # 格式化
swapon /dev/sda5    # 启动swap设备
# 设置开机自动启动：将以下信息写入/etc/fstab文件中
# 注意：因为内存交换分区没有挂载点，所以第二栏填写swap
UUID="21ef885b-2fd2-4afe-ae45-3916001c3b67" swap swap defaults 0 0
swapon -a           # 更新/etc/fstab配置文件
# 观察状态。使用free与swapon -s命令观察内存的使用量
free # 列出磁盘所有的分区信息，但不显示loop设备类型的交换分区
# Mem为磁盘主要内存，Swap为交换分区信息。信息被分为6栏。buffers为缓存占用量
```

:::
::: tab "第二种：使用文件创建内存交换分区"

- 第一步：使用 dd 命令创建一个新文件
- 第二步：格式化。使用 mkswap 对分区进行格式化
- 第三步：启动 swap 设备。使用 swapon 命令
- 第四步（自选）：设置开机自动启动。将信息写入/etc/fstab 文件中

```shell
# 第一步：使用dd命令创建一个新文件。此处我在/tmp目录下建立一个swap文件，大小为128M
dd if=/dev/zero of=/tmp/swap bs=1M count=128
# 第二步：格式化。使用mkswap对分区进行格式化
mkswap /tmp/swap
# 第三步：启动swap设备。使用swapon将/tmp/swap启动
swapon /tmp/swap
# 第四步(自选)：设置开机自动启动。将以下信息写入/etc/fstab文件中
/tmp/swap swap swap defaults 0 0
swapon -a     # 然后再更新/etc/fstab配置文件
# 注意：第一栏一定要使用文件名而不使用UUID，因为系统仅会查询区块设备不会查询文件
# 并且内存交换分区没有挂载点，所以第二栏填写swap
```

:::
::::
