---
title: HTML基础语法
date: 2022-06-01 14:14:06
permalink: /pages/0b7ef1/
categories:
  - 前端
tags:
  - HTML
---

## HTML介绍

:::: tabs cache-lifetime="5" :options="{ useUrlFragment: false }"
::: tab 元素
> 一个HTML元素分为三部分：开始标签、结束标签、标签之间的内容。元素名不区分大小写，但常使用小写

元素类型分类：
- 空元素：元素的开始和结束标签之间并非一定要有内容，没有内容的元素称为空元素
  - 例如：`<code></code>`，但元素为空时没有意义
- 自闭合标签：空元素可以更简洁地只用一个标签表示
  - 例如：`<code/>`
- 虚元素：有些元素只能使用一个标签表示，在其中放置任何内容都不符合HTML规范。虚元素有两种表示法
  - 第一种只使用开始标签，例如：`<hr>`。浏览器知道hr是虚元素，所以不会等待其结束标签出现
  - 第二种表示法在此基础上加了一个斜杠符号，其形式与空元素一致。例如：`<hr />`

![HTML元素](https://cdn.jsdelivr.net/gh/sbgong/static/blog/frontend/html/202206031401170.png)
:::
::: tab 元素属性
元素可以用属性（attribute）进行配置
- 属性只能用在开始标签或单个标签上，不能用于结束标签
- 属性具有名称和值两部分

![元素属性](https://cdn.jsdelivr.net/gh/sbgong/static/blog/frontend/html/202206031402493.png)
:::
::: tab 转义字符

:::
::: tab 全局属性

:::
::::












