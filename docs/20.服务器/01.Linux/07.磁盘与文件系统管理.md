---
title: 磁盘与文件系统管理
date: 2022-04-30 15:18:17
permalink: /pages/35e63e/
categories:
  - 服务器
tags:
  - Linux
---

## Linux 文件系统

文件系统是操作系统中负责管理持久数据的子系统，说简单点，就是负责把用户的文件存到磁盘硬件中，因为即使计算机断电了，磁盘里的数据并不会丢失，所以可以持久化的保存文件

文件系统的基本数据单位是文件，它的目的是对磁盘上的文件进行组织管理，那组织的方式不同，就会形成不同的文件系统

### 文件系统特性

磁盘分区完毕后需要进行格式化（format），之后操作系统才能够使用这个文件系统。『为什么需要进行格式化磁盘？』因为每种操作系统所设定的文件属性/权限并不相同，为了存放这些文件所需的数据，因此就需要将分区槽进行格式化，已成为操作系统能够利用的文件系统格式

每种操作系统能够使用的文件系统并不相同，Windows98 以前的微软操作系统主要利用的文件系统是 FAT，Windows2000 以后的版本是 NTFS 文件系统。而 Linux 的正统文件系统是 Ext2。在默认情况下，Windows 系统是不会认识 Ext2 的

传统的磁盘与文件系统应用中，一个分区槽就只能够被格式化成一个文件系统。但由于新技术的应用，例如 LVM 与软件磁盘阵列，这些技术可以将一个分区槽格式化为多个文件系统（例如 LVM），也能够将多个分区槽合并成一个文件系统（LVM、RAID）。所以说，目前**可以称一个被挂载的数据为一个文件系统**

::: info 索引式文件系统
操作系统的文件数据除了文件实际内容外，通常含有非常多的属性，文件系统通常会将这两部分的数据分别存放在不同的区块，权限与属性放置到 inode 中，实际数据则放到 data block 区块中。另外还有一个超级区块（superblock）会记录整个文件系统的整体信息，包括 inode 与 block 的总量、使用量、剩余量等（每个 inode 与 block 都有编号）

- `superblock`：记录此文件系统的整体信息，包括 inode/block 的总量、使用量、剩余量以及文件系统的格式与相关信息等
- `inode`：记录文件的属性，一个文件占用一个 inode，同时记录此文件的数据所在的 block 号码
- `block`：实际记录文件的内容，若文件太大时，会占用多个 block

由于每个 inode 和 block 都有编号，而每个文件都会占用一个 inode，inode 内有文件数据位置的 block 号码，因此如果找到文件的 inode 的话，那么自然就会知道这个文件所放置数据的 block 号码，当然也就能够读出该文件的实际数据了。这种数据读取的方法被称为**索引式文件系统**
:::

而 FAT 格式的文件系统(例如随身碟)并没有 inode 的存在，所以 FAT 无法将一个文件的所有 block 在一开始就读取出来。FAT 的每个 block 号码都记录在前一个 block 当中，它需要一个一个的将 block 读出后，才会知道下一个 block 在何处。『碎片整理』就是因为文件写入的 block 太过于分散，此时文件读取的效能就会变得很差，这个时候可以通过碎片整理将同一个文件所属的 blocks 汇集在一起，这样的数据读取会比较容易。FAT 文件系统需要经常的碎片整理一下，而 Ext2 是索引式文件系统，不太需要碎片整理

### EXT2 文件系统

![ext2文件系统](https://cdn.jsdelivr.net/gh/sbgong/static/blog/server/linux/202205021443326.png)

::: info Linux 文件系统 Ext2 文件系统

- inode 的内容在记录文件的权限与相关属性，而 block 区块则是在记录文件的实际内容。而且文件系统在格式化之后就将 inode 和 block 规划好了，除非重新格式化（或者利用 resize2fs 等指令变更文件系统大小），否则 inode 和 block 固定之后就不再变动
- 但是如果文件系统高达数百 GB 时，将所有的 inode 和 block 统统放在一起不太合理，因为 inode 和 block 的数量太庞大，不容易管理。为此，Ext2 文件系统在格式化的时候基本上是区分为多个区块群组（block group），每个区块群组都有独立的 inode/block/superblock 系统
- 而在整体规划磁盘的时候，文件系统最前面有一个启动扇区（boot sector），这个启动扇区可以安装开机管理程序，如此一来就能够将不同的开机管理程序安装到个别的文件系统最前面，而不用覆盖整个磁盘的 MBR，这样也才能够制作出多重引导的环境

:::

每一个区块群组有六个主要内容：

:::: tabs cache-lifetime="5" :options="{ useUrlFragment: false }"
::: tab data-block
这是用来放置文件内容数据的地方，在 Ext2 文件系统中所支持的 block 大小有 1K/2K/4K 三种。在格式化的时候 block 大小就固定了，且每个 block 都有编号，方便 inode 的记录。注意：由于 block 大小的差异，会导致该文件系统能够支持的最大磁盘容量与最大单一文件容量并不相同。因此 block 大小而产生的 ext2 文件系统限制如下：

|     block 大小     | 1KB  |  2KB  | 4KB  |
| :----------------: | :--: | :---: | :--: |
|  最大单一文件限制  | 16GB | 256GB | 2TB  |
| 最大文件系统总容量 | 2TB  |  8TB  | 16TB |

- 原则上，block 的大小与数量在格式化完就不能够再改变了（除非重新格式化）
- 每个 block 内最多只能够放置一个文件的数据
- 如果文件大于 block 的大小，则一个文件会占用多个 block 数量；如果文件小于 block，则该 block 的剩余容量就不能够再被使用了（磁盘空间会浪费）

:::
::: tab inode-table
系统读取文件时需要先找到 inode，并分析它记录的权限与用户是否符合，若符合才能够开始实际读取 block 的内容。inode 记录的文件数据至少有底下这些：

- 文件的存取权限（read/write/execute）
- 文件的拥有者和群组
- 文件的容量
- 文件建立或状态改变的时间（ctime）
- 最近一次的读取时间（atime）
- 最近一次修改文件内容的时间（mtime）
- 定义文件的特殊权限，如 SUID
- 该文件真正内容的指向

inode 要记录的数据非常多，但 inode 的大小仅仅只有 128bytes，而 inode 记录一个 block 号码要花掉 4bytes，假设一个文件有 400M 且每个 block 为 4KB，那么至少也在 10 万条 block 号码的记录，inode 哪能记录这么多信息呢，inode 空间也不够啊；因此系统将 inode 记录 block 号码的区域定义为 12 个直接、一个间接、一个双间接与一个三间接记录；

- 直接就是直接指向 block 号码
- 间接就是拿一个 block 来当作记录 block 号码的记录区；如果文件太大时，就会使用间接的 block 来记录编号

![block](https://cdn.jsdelivr.net/gh/sbgong/static/blog/server/linux/202205021444521.png)

假设每个 block 的大小为 1KB:

- 12 个直接：12 x 1=12K;
- 一个间接：256 x 1K=256K; 256=1KB(一个 block) / 4byte(记录一个 block 索引需要花费的大小)
- 一个双间接：256 x 256 x 1KB=256^2K
- 一个三间接：256 x 256 x 256 x 1KB=256^3K

总共加起来为：16GB；所以当文件系统 block 格式化为 1K 大小时，能够容纳的最大文件为 16GB
:::
::: tab super-block
超级区块是记录整个文件系统相关信息的地方，一般来说，superblock 的大小为 1024bytes。一个文件系统应该只有一个 superblock，如果其他组中也有 superblock，那主要是作为第一个 block group 内 superblock 的备份

- block 和 inode 的总量
- 未使用与已使用的 inode/block 数量
- block 与 inode 的大小（block 为 1/2/4K，inode 为 128bytes 或 256bytes）
- 文件系统的挂载时间、最近一次写入数据的时间、最近一次检验磁盘的时间（fsck）等文件系统的相关信息
- 一个`valid bit`数值，若此文件系统已被挂载，则该值为 0；若未被挂载，则该值为 1

:::
::: tab file-system-description
**文件系统描述说明**：这个区段可以描述每个 block group 的开始与结束的 block 号码，以及说明每个区段分别介于哪一个 block 号码之间
:::
::: tab block-bitmap
**区块对照表**：从这里可以知道哪些 block 是空的，因此我们的系统可以很快的找到使用的空间来存储文件；同样的，如果你要删除某些文件时，那些文件原本占用的 block 号码就得要释放出来，此时在 block bitmap 当中相对应到该 block 号码的标志就得修改为『未使用中』
:::
::: tab inode-bitmap
**inode 对照表**：这是记录使用与未使用的 inode 号码
:::
::::

### xfs文件系统

xfs是一个日志式文件系统，在资料的分布上，主要规划为三个部分：数据区（data section）、文件系统活动登录区（log section）以及实时运作区（realtime section）

:::: tabs cache-lifetime="5" :options="{ useUrlFragment: false }"
::: tab 数据区
数据区和ext文件系统一样，包括inode/data block/superblock等数据，都放置在这个区块。这个数据区和ext文件系统的block group类似，也是分为多个群组（allocation groups）来分别放置文件系统所需要的数据。每个群组都包含了
- 整个文件系统的superblock
- 剩余空间的管理机制
- inode的分配与追踪

此外inode与block都是系统用到时，才动态配置产生的，所以格式化动作超级快。另外与ext文件系统不同的是，xfs的block与inode有多种不同的容量可供设定，block容量由512字节-64KB调配。不过，Linux环境下，由于内存控制的关系，最高可使用的block大小为4K，至于inode容量可由256bytes到2M这么大，还是保留256字节的默认值就够用了
:::
::: tab 文件系统活动登录区
这个区域主要被用来记录文件系统的变化，有点像是日志区。文件的变化会在这里记录下来，直到该变化完整的写入到数据区后，该笔记录才会被终结。如果文件系统因为某些缘故（例如停电）而损毁时，系统会拿这个登录区块来进行校验，看看系统挂掉之前，文件系统正在运作些什么动作，借以快速的修复文件系统
:::
::: tab 实时运作区
当有文件要被建立时，xfs会在这个区段里面找一个到数个extend区块，将文件放置在这个区块内，等到分配完毕后，再写入到data section的inode与block去。这个extend区块的大小得要再格式化的时候就先指定，最小值是4K最大可到1G。这个extend最好不要乱动，因为可能会影响到实体磁盘的效能
:::
::::

## 文件系统的简单操作

### 磁盘与目录的容量

















## 磁盘的分区/格式化/检验与挂载

## 设置开机挂载

## 内存置换空间：swap
