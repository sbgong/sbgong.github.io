---
title: 视觉格式化基础
date: 2022-07-09 09:19:01
permalink: /pages/27fc25/
categories:
  - 前端
tags:
  - CSS
---

## 元素框基础

不管是什么元素，CSS 都假定每个元素都生成一个或多个矩形框，我们称之为元素框（以后的规范可能会允许元素生成非矩形框，而其实现在就有这方面的提议，不过目前生成的框都是矩形的）

各元素框的中心是内容区域，四周有可选的内边距、边框、轮廓和外边距。之所以说这些区域是可选的，是因为它们的宽度都可以设为零，即把它们从元素框上删除

外边距、边框和内边距都有分别针对每一边的属性，例如 margin-left 或 border-bottom，也有简写属性，例如 padding。而轮廓没有针对各边的属性

默认情况下，内容区的背景出现在内边距范围内。外边距区域始终是透明的，因此透过它能看到父元素。**内边距不能为负值，但是外边距可以**

边框的线型由样式定义，例如设为 solid 或 inset，边框的颜色由 border-color 属性设定。如未设定颜色，边框的颜色与元素中内容的前景色相同。比如说，段落中的文本是白色的，如果创作人员没有明确声明使用其他颜色，那么段落四周的边框也是白色的。如果边框的线型有间隙，那么默认情况下，从间隙能看到元素的背景。最后，**边框的宽度不能为负数**

## 重要概念概览

:::: tabs cache-lifetime="5" :options="{ useUrlFragment: false }"
::: tab "常规流动"
即渲染西方语言时从左至右、从上到下的顺序，以及传统的 HTML 文档采用的文本布局方式。注意，对非西方语言来说，流动方向可能会变。多数元素都采用常规的流动方式，除非元素浮动了、定位了，放入弹性盒或采用栅格布局了
:::
::: tab "非置换元素"
内容包含在文档中的元素。例如：段落（p）是非置换元素，因为段落中的文本内容在元素自身中
:::
::: tab "置换元素"
为其他内容占位的元素。典型的置换元素是 img，它指向一个图像文件，那个图像就插在 img 元素在文档流中的位置上。多数表单元素也是置换元素（例如`<input type="radio">`）
:::
::: tab "根元素"
位于文档树顶端的元素。在 HTML 文档中，根元素是 html。在 XML 文档中，根元素可以是语言允许的任何元素，例如，RSS 文件的根元素是 rss
:::
::: tab "块级框"
段落、标题或 div 等元素生成的框。在常规流动模式下，块级框在框体前后都“换行”，因此块级框是纵向堆叠的。`display: block` 声明能把任何元素生成的框体变成块级框
:::
::: tab "行内框"
strong 或 span 等元素生成的框体。行内框前后不“换行”。`display:inline` 声明能把任何元素生成的框体变成行内框
:::
::: tab "行内块级框"
内部特征像块级框，外部特性像行内框。行内块级框的行为与置换元素相似，但不完全相同。比如说把一个 div 元素像行内图像那样插入一行文本，这样一想你就明白了
:::
::: tab "容纳块"
每个元素的框体相对容纳块放置，简单说就是，容纳块是元素框体的『布局上下文』。为了确定框体的容纳块，CSS 定义了一系列规则。在使用常规流动方式渲染的西方文本中，容纳块由离元素最近的那个生成列表项目或块级框的祖辈元素的边界构成

```html
<body>
  <div>
    <p>This is a paragraph.</p>
  </div>
</body>
```

在这段十分简单的标记中，p 元素的块级框的容纳块是 div 元素的块级框，因为这是祖辈元素的框体中离 p 元素最近的，而且生成的是块级框或列表项目（这里 div 元素生成的是块级框）。类似的，div 元素的容纳块是 body 元素的框体。因此，p 元素的布局依赖 div 元素的布局，而 div 元素的布局又依赖 body 元素的布局。继续下去，body 元素的布局依赖 html 元素的布局，html 元素的布局对应的是初始容纳块。初始容纳块的独特之处是其尺寸由视区决定（视区：屏幕媒体中的浏览器窗口，印刷媒体中的可打印区域），而非根元素中内容的尺寸
:::
::::

## 调整元素的显示方式：display

为 `display` 属性设值可以影响用户代理显示元素的方式

![display](https://cdn.jsdelivr.net/gh/sbgong/static/blog/frontend/css/202207091512657.png)

|        属性值        | 说明                                                                           |
| :------------------: | ------------------------------------------------------------------------------ |
|        `none`        | 隐藏对象。与 visibility 属性的 hidden 值不同，其不为被隐藏的对象保留其物理空间 |
|       `inline`       | 指定对象为内联元素                                                             |
|       `block`        | 指定对象为块元素                                                               |
|    `inline-block`    | 指定对象为内联块元素 <Badge text="CSS2" />                                     |
|     `list-item`      | 指定对象为列表项目                                                             |
|       `table`        | 指定对象作为块元素级的表格。类同于 html 标签`<table>` <Badge text="CSS2" />    |
|    `inline-table`    | 指定对象作为内联元素级的表格。类同于 html 标签`<table>` <Badge text="CSS2" />  |
|   `table-caption`    | 指定对象作为表格标题。类同于 html 标签`<caption>` <Badge text="CSS2" />        |
|     `table-cell`     | 指定对象作为表格单元格。类同于 html 标签`<td>` <Badge text="CSS2" />           |
|     `table-row`      | 指定对象作为表格行。类同于 html 标签`<tr>` <Badge text="CSS2" />               |
|  `table-row-group`   | 指定对象作为表格行组。类同于 html 标签`<tbody>` <Badge text="CSS2" />          |
|    `table-column`    | 指定对象作为表格列。类同于 html 标签`<col>` <Badge text="CSS2" />              |
| `table-column-group` | 指定对象作为表格列组显示。类同于 html 标签`<colgroup>` <Badge text="CSS2" />   |
| `table-footer-group` | 指定对象作为表格标题组。类同于 html 标签`<thead>` <Badge text="CSS2" />        |
| `table-header-group` | 指定对象作为表格脚注组。类同于 html 标签`<tfoot>` <Badge text="CSS2" />        |
|       `run-in`       | 根据上下文决定对象是内联对象还是块级对象 <Badge text="CSS3" />                 |
|        `box`         | 将对象作为弹性伸缩盒显示（伸缩盒最老版本） <Badge text="CSS3" />               |
|     `inline-box`     | 将对象作为内联块级弹性伸缩盒显示（伸缩盒最老版本） <Badge text="CSS3" />       |
|      `flexbox`       | 将对象作为弹性伸缩盒显示（伸缩盒过渡版本） <Badge text="CSS3" />               |
|   `inline-flexbox`   | 将对象作为内联块级弹性伸缩盒显示（伸缩盒过渡版本） <Badge text="CSS3" />       |
|        `flex`        | 将对象作为弹性伸缩盒显示（伸缩盒最新版本） <Badge text="CSS3" />               |
|    `inline-flex`     | 将对象作为内联块级弹性伸缩盒显示（伸缩盒最新版本） <Badge text="CSS3" />       |

**注意**：我们改变的是元素的显示方式，而不是元素的本性。也就是说，把段落生成的框体变成行内框并不会把段落变成行内元素。例如：HTML 元素有些是块级元素，有些则是行内元素。行内元素可以作为块级元素的后代，但是反过来一般不行。span 元素可以放在一个段落中，但是却不能把段落放在 span 元素中。不论怎样装饰元素，这一点都不成立。display 属性的名称就表明，它是影响元素显示方式的，而不能改变元素的种类

## 块级框

![块级框](https://cdn.jsdelivr.net/gh/sbgong/static/blog/frontend/css/202207121702021.png)

## 调整块级框：box-sizing

默认情况下块级框的宽度（width）等于左内边界到右内边界的距离，高度（height）等于上内边界到下内边界的距离，这两个属性可用于生成块级框的元素。这些块级框的属性可以使用 `box-sizing` 属性进行调整，用于改变 width 和 height 值的具体意义

| 功能   | 说明                                     |
| ------ | ---------------------------------------- |
| 取值   | `content-box / padding-box / border-box` |
| 初始值 | content-box                              |
| 适用于 | 能设定 width 或 height 的所有元素        |
| 继承性 | 否                                       |
| 动画性 | 否                                       |

- content-box：如果声明 `width: 400px;`，那么元素的内容框将为 400px，内边距和边框等都在此基础上增加
- border-box：如果声明 `width: 400px;`，那么元素框从左边框的外边界到有边框的外边界相距 400px

![box-sizing](https://cdn.jsdelivr.net/gh/sbgong/static/blog/frontend/css/202207201506404.png)

## 横向格式化属性

横向格式化属性有七个，这七个属性影响块级框的横向布局：

`margin-left`、`border-left`、`padding-left`、`width`、`padding-right`、`border-right`、`margin-right`

- 这七个属性的值加在一起要等于元素容纳块的宽度，而这一宽度通常为块级元素的父元素的 width 值（因为块级元素的父元素几乎都是块级元素）
- 在这七个属性中，只有三个属性的值能设为 `auto`：`width`、`margin-left`和`margin-right`。余下的几个属性，要么设为具体的值，要么使用默认值（零）
- `border`、`padding` 值均为非负值；只有 `margin` 可以是负值

外边距可以设置为负值。如果七个横向属性之和始终等于父元素的 width，只要这几个属性的值都大于或等于零，元素就不可能比父元素的内容还宽。然而，当外边距出现负值之后，就会出现子元素比父元素宽的现象了

```
div {width: 500px; border: 3px solid black;}
p.wide {margin-left: 10px; width:auto; margin-right: -50px;}
```

## 横向格式化属性使用 auto 值

:::: tabs cache-lifetime="5" :options="{ useUrlFragment: false }"
::: tab "单个 auto"
在 width、margin-left、margin-right 三个属性中，如果把其中一个设为 auto，另两个设为具体的值，那么设为 auto 的那个属性的具体长度要能满足元素框的宽度等于父元素的宽度。从某种意义上说，auto 可用于补全总和所缺的尺寸

假如七个属性的值之和必须等于 500 像素，margin-right 为 100 像素，width 为 100 像素，margin-left 设为 auto，而且没有设定内边距或边框，那么左外边距的宽度将是 300 像素

```
div { width: 500px; }
p { margin-left: auto; margin-right: 100px; width: 100px; }
/* 设为auto的左外边距最终计算为300px */

p { margin-left: 100px; margin-right: 100px; }
等价于
p { margin-left: 100px; margin-right: 100px; width: auto; }
```

然而，如果把这三个属性的值都设为具体的数值，用 CSS 术语来说就是过约束了，那么 margin-right 将被强制设为 auto。也就是说，如果左右外边距和宽度都设为 100px 的话，用户代理会把右外边距重置为 auto。此时，右外边距的宽度自动设定，不过要满足元素的总宽度等于容纳块的宽度

```
div { width: 500px; }
p { margin-left: 100px; margin-right: 100px; width: 100px; }
/* 右外边距被强制设为300px */
```

:::
::: tab "多个 auto"
如果左右外边距都明确设为 auto，那么外边距的长度相等，元素在父元素内居中显示

```
div {width: 500px;}
p {width:300px; margin-left: auto; margin-right: auto;}
/* 两侧的外边距都是100像素，因为 (500-300)/2 = 100 */
```

此外，还可以把某一边的外边距和 width 设为 auto，此时，设为 auto 的那个外边距等于 0

```
div {width: 500px;}
p {margin-left: auto; width:auto; margin-right: 100px;}
/* 左外边距的计算结果为0，width则等于400px */
```

最后，如果这三个属性都设为 auto 呢？答案是，两侧的外边距被设为零，而 width 则要多宽有多宽。这跟默认情况是一样的，即两侧的外边距和宽度都不明确声明值，此时外边距默认为零，而 width 默认为 auto
:::
::: tab "置换元素的 auto"
针对块级置换元素，width 为 auto 时，置换元素的 width 等于内容自身的宽度；如果明确为 width 提供一个值，可以覆盖这个规则，且元素的高度随之也增加了。如果置换元素的 width 与自身宽度不同，height 的值会按比例变化，除非明确设定 height 的值。反过来亦然，如果设定了 height，而把 width 设为 auto，那么宽度会随着高度按比例变化
:::
::::

## 纵向格式化

块级框的纵向格式化与横向格式化类似。其中元素的内容决定元素的默认高度，内容的宽度对高度也有影响，例如：段落越窄，为了包含内部的所有内容，高度就越高

通过 CSS 可以为任何块级元素设定具体的高度，这么做得到的结果取决于多个因素

- 假设指定的高度大于显示内容所需的高度。此时多出的高度看起来像是内边距
- 假设指定的高度小于显示内容所需的高度。那么元素的内容将会比框体高，用户代理的具体行为取决于 overflow 属性

## 纵向格式化属性

纵向格式化也涉及七个属性，这七个属性的值加在一起必须等于块级框的容纳快的高度。通常，这是块级框父元素的 height 值（因为块级元素的父元素几乎都是块级元素）

`margin-top`、`border-top`、`padding-top`、`height`、`padding-bottom`、`boder-bottom`、`margin-bottom`

- 这七个属性中只有三个可以设为 auto 的：`height`、`margin-top`、`margin-bottom`
- height 属性要么设为 auto，要么设为某种类型的非负值，绝不能小于零
- 上下内边距和边框必须设为具体的值，否则取默认值零
- 在常规流动模式下，如果把块级框的 margin-top 或 margin-bottom 设为 auto，二者都自动计算为 0。如此看来，常规流动模式下的元素无法轻易在容纳快中纵向居中

**自动调整高度**：在常规流动模式下，声明 `height: auto` 的块级框是最简单的，此时框体的高度恰好能放得下里面的内容。常规流动模式下的块级框如果高度是自动调整的，而且子代都是块级元素，那么默认的高度是从最上边那个块级子代元素的上边框外侧到最下边那个块级框子代元素的下边框外侧之间的距离。然而，如果块级元素有上内边距或下内边距，或者有上边框或下边框，那么其高度是从最上边那个子元素的上外边距的外边界到最下边那个子元素的下外边距的外边界之间的距离

## 外边距的合并与塌陷

纵向格式化的另一个重要特征是：**相邻的纵向外边距会折叠**，只有外边距有这种折叠行为

通过纵向罗列的无序列表最容易说明外边距折叠行为。假设把下述声明应用到一个有五个项目的列表上，每个列表项目都有 10 像素的上外边距和 15 像素的下外边距。然而，渲染时，相邻的两个列表项目之间的距离是 15 像素，而不是 25 像素。这是因为，相邻的外边距在纵轴上折叠了。换句话说就是，较小的外边距被较大的外边距消去了。目前实现的用户代理都会折叠相邻的纵向外边距

```
li {margin-top: 10px; margin-bottom: 15px;}
```

负外边距对纵向格式化有一定影响，比如会影响外边距的折叠。如果两个相邻的外边距都是负值，浏览器取其中绝对值较大的那个，然后从正外边距中减去它的绝对值。也就是说，把负值与正值相加，得到的结果为两个元素之间的距离。注意：上下外边距为负时出现的『上移』现象，这与横向外边距为负时从父元素中溢出没什么区别

:::: tabs cache-lifetime="5" :options="{ useUrlFragment: false }"
::: tab "外边距合并"
相邻块元素垂直外边距的合并。当上下相邻的两个块元素（兄弟关系）相遇时，如果上面的元素有下外边距 margin-bottom，下面的元素有上外边距 margin-top ，则它们之间的垂直间距不是 margin-bottom 与 margin-top 之和。取两个值中的较大者这种现象被称为相邻块元素垂直外边距的合并

解决方案：尽量只给一个盒子添加 margin 值

![外边距合并](https://cdn.jsdelivr.net/gh/sbgong/static/blog/frontend/css/202207122118439.png)
:::
::: tab "外边距塌陷"
嵌套块元素垂直外边距的塌陷。对于两个嵌套关系（父子关系）的块元素，父元素有上外边距同时子元素也有上外边距，此时父元素会塌陷较大的外边距值

解决方案：可以为父元素添加 `overflow:hidden`

![外边距塌陷](https://cdn.jsdelivr.net/gh/sbgong/static/blog/frontend/css/202207122119653.png)
:::
::::

## 行内元素及其相关术语

除块级元素之外，行内元素是最常见的。行内元素的相关属性能实现更有趣的效果。常见的行内元素有 em 和 a 等非置换元素，以及置换元素——图像。注意，本节所述的行为均不适用于表格元素。表格及其内容的行为与块级元素和行内元素有极大的差异，因此 CSS2 引入了专门的新属性和行为。本书不讨论表格装饰，因为这是一个全新的领域，而且异常繁琐

[相关术语请结合行的构成进行学习](/pages/6882f8/#行的构成)

|   术语   | 说明                                                                                                                                                                                                                                                   |
| :------: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 匿名文本 | 不在任何行内元素中的字符串。注意：空格也是匿名文本的一部分，因为空格也是一种字符。例如：`<p>I'm <em>so</em> happy!</p>` 其中 "I'm" 和 "happy!" 是匿名文本                                                                                              |
|  字体框  | 由字体具体定义，也叫字符框。字形可能比字体框高或矮。CSS 中的 font-size 属性控制字体框的高度                                                                                                                                                            |
|  内容区  | 对非置换元素来说，内容区有两种定义，CSS 规范允许用户代理选择其中一个实现。内容区可以是各字符的字体框连在一起构成的方框，也可以是元素中各字符的字形合在一起构成的方框。简单起见，本书采用前者。对置换元素来说，内容区是元素自身的高度加上内外边距和边框 |
|   行距   | 行距是 font-size 和 line-height 之差。二者之间的差距除以 2 之后分别添加到内容区的上部和下部。内容区多出来的这部分空间叫半行距。只有非置换元素有行距                                                                                                    |
|  行内框  | 内容区加行距后得到的方框。对非置换元素来说，行内框的高度正好等于 line-height 属性的值。对置换元素来说，行内框的高度等于内容区的高度，因为置换元素没有行距。注意：行内框在一行中纵向对齐的方式由 `vertical-align` 属性决定                              |
|   行框   | 过一行中各行内框最高点和最低点的方框。也就是说，行框的顶边过最高那个行内框的顶边，行框的底边过最低那个行内框的底边                                                                                                                                     |

## 行内置换元素

行内置换元素自身是有宽度和高度的。置换元素的行内框包含整个元素：内容、内边距、边框和外边距
