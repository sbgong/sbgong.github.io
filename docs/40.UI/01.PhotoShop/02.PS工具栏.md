---
title: PS工具栏
date: 2022-07-10 21:47:27
permalink: /pages/5b1c4c/
categories:
  - UI
tags:
  - PhotoShop
---

|       工具       | 快捷键 | 说明                                 |
| :--------------: | :----: | ------------------------------------ |
|     移动工具     |  `v`   | 移动选区或图层                       |
|   矩形选框工具   |  `m`   | 创建矩形形状的选区                   |
|     套索工具     |  `l`   | 创建手绘选区                         |
|   对象选择工具   |  `w`   | 查找并自动选择对象                   |
|     裁剪工具     |  `c`   | 裁切或扩展图像的边缘                 |
|     图框工具     |  `k`   | 为图像创建占位符图框                 |
|     吸管工具     |  `i`   | 从图像中取样颜色                     |
| 污点修复画笔工具 |  `j`   | 移去标记和污点                       |
|     画笔工具     |  `b`   | 绘制自定义画笔描边                   |
|   仿制图章工具   |  `s`   | 使用来自图像其他部分的像素绘画       |
| 历史记录画笔工具 |  `y`   | 建图像的某些部分恢复到以前的状态     |
|    橡皮擦工具    |  `e`   | 将像素更改为背景颜色，或者使它们透明 |
|     渐变工具     |  `g`   | 创建颜色之间的渐变混合               |
|     模糊工具     |        | 模糊图像中的区域                     |
|     减淡工具     |  `o`   | 调亮图像中的区域                     |
|     钢笔工具     |  ` p`  | 通过锚点与手柄创建和更改路径或形状   |
|   横排文字工具   |  `t`   | 添加横排文字                         |
|   路径选择工具   |  `a`   | 选择整个路径                         |
|     矩形工具     |  `u`   | 绘制矩形                             |
|     抓手工具     |  `h`   | 在图像的不同部分间平移               |
|     缩放工具     |  `z`   | 在图像上放大和缩小                   |

## 移动工具组：V

> 移动工具组包括：移动工具、画板工具

PS 自动勾选了属性栏中的 `自动选择`，这样用户就可以随意选择不同图层的图形。如果取消勾选了该选项，则你只能拖动当前图层的图形，当然你也可以使用 <kbd>Ctrl</kbd> + <kbd>鼠标左键</kbd> 拖动其他图层的图形

## 选区工具组

- 矩形选框工具（M）：矩形选框工具、椭圆选框工具、单行选框工具、单列选框工具
- 套索工具（L）：套索工具、多边形套索工具、磁性套索工具
- 对象选择工具（W）：对象选择工具、快速选择工具、魔棒工具

如果要在 Photoshop 中处理图像的局部效果，就需要为图像指定一个有效的编辑区域，这个区域就是 PS 选区。通过选择特定区域，可以对该区域进行编辑并保持未选定区域不被改动。另外，使用选区可以将对象从一张图像中分离出来。注意，将选区中的图像分离出来，必须选择『移动工具』

![属性栏](https://cdn.jsdelivr.net/gh/sbgong/static/blog/ui/ps/202207121222038.png)

- 新选区：默认选中；激活该按钮以后，可以创建一个新选区。如果已经存在选区，那么新创建的选区将替代原来的选区
- 添加到选区：激活该按钮以后，可以将当前创建的选区添加到原来的选区中（按住 <kbd>Shift</kbd> 键也可以实现相同的操作）
- 从选区减去：激活该按钮以后，可以将当前创建的选区从原来的选区中减去（按住 <kbd>Alt</kbd> 键也可以实现相同的操作）
- 与选区交叉：激活该按钮以后，新建选区时只保留原有的选区与新创建的选区相交的部分（按住 <kbd>Alt</kbd> + <kbd>Shift</kbd>键也可以实现相同的操作）
- 反选：创建一个选区之后，按住 <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>i</kbd> 键，即可反选该选区之外的内容
- 羽化选区：羽化原理是令选区内外衔接部分虚化，起到渐变的作用从而达到自然衔接的效果
  - 使用方法一：先调整好羽化数值，然后再新建选区
  - 使用方法而：已经新建好选区之后还想羽化，就需要使用快捷键 <kbd>Shift</kbd> + <kbd>F6</kbd> 调出羽化参数窗口进行设置
- 取消选区快捷键：<kbd>Ctrl</kbd> + <kbd>Delete</kbd> 键；重新调出选区快捷键：<kbd>Ctrl</kbd> + <kbd>鼠标左键</kbd> 点击想要的选区图层即可

## 对象选择工具组：

## 裁剪工具组：

## 图框工具组：

## 吸管工具组：

## 污点修复画笔工具组：J

> 污点修复画笔工具组包括：污点修复画笔工具、修复画笔工具、修补工具、内容感知移动工具、红眼工具

PS 修复工具用于对图像细微部分进行修整，是在处理图像时不可缺少的工具之一

- 污点修复画笔工具：使用污点修复画笔工具可以快速消除图像中的污点和某个对象。它不需要设置取样点，因为它可以自动从所修饰区域的周围进行取样。调整修补范围的快捷键：<kbd>Alt</kbd> + <kbd>鼠标右键</kbd> + <kbd>鼠标左右移动</kbd> 来缩小和扩大范围（左小右大）
- 修复画笔工具：按住 <kbd>Alt</kbd> 键，然后选择好的地方，进行选取样本，然后松开 <kbd>Alt</kbd> 键，点击污点地方，就能将污点进行消除，有点类似图章的效果。还能调整选择的硬度：<kbd>Alt</kbd> + <kbd>鼠标右键</kbd> + <kbd>鼠标上下移动</kbd>（上软下硬）
- 修补工具：你自己手动选择污点的区域，然后把它拖到好的地方去
- 内容感知移动工具：圈选图像部分，把它移动到指定的位置（相当于剪切），并且它会自动内容感知指定位置的周围，让这两部分尽可能融合在一起。建议：刚开始选择图像的时候，尽量较小圈选图像的周围，这样有利于后面的融合；还可以把结构属性调成最小、颜色属性调为最大，也更有利于融合
- 红眼工具：就是去除眼睛的红色

## 画笔工具组：B

> 画笔工具组包括：画笔工具、铅笔工具、颜色替换工具、混合器画笔工具

- 铅笔工具处理不能调节硬度，其他和画笔工具差不多
- 颜色替换工具：它是用来替换一开始吸取的颜色的。比如想替换一张图片的背景色颜色时，这个非常方便
- 混合器画笔工具：比如用来融合两个颜色的，让两个颜色之间能够非常好的过渡

画笔工具属性中有非常多的设置：

:::: tabs cache-lifetime="5" :options="{ useUrlFragment: false }"
::: tab "画笔笔尖形状"

:::
::: tab "形状动态"

:::
::: tab "散布"

:::
::: tab "纹理"

:::
::: tab "双重画笔"

:::
::: tab "颜色动态"

:::
::: tab "传递"

:::
::: tab "画笔笔势"

:::
::: tab "杂色、湿边、建立、平滑、保护纹理"
这 5 个选项不能调整参数，如果要启动，直接勾选即可
:::
::::

## 仿制图章工具组：S

> 仿制图章工具组包括：仿制图章工具、图案图章工具

使用 PS 仿制图章工具，可以将图像的一部分复制到同一图像的另一个位置上，或绘制到具有相同颜色模式的任何打开的文档的另一部分，当然也可以将一个图层的一部分绘制到另一个图层上

- 仿制图章工具应用前，需要按住<kbd>Alt</kbd>键，定义取样点
- 选定仿制源后，松开<kbd>Alt</kbd>键。把光标移到你所要修补的地方，重复点击鼠标，直到你所希望的效果出现
- 注意：默认放置图章工具勾选了『对齐』属性，它能按原图比例将整图复制出来

## 历史记录画笔工具组：Y

> 历史记录画笔工具组包括：历史记录画笔工具、历史记录艺术画笔工具

历史记录画笔工具，就是可以将当前图层进行擦除，擦除到历史记录最上层/最开始记录的那个状态

## 橡皮擦工具组：E

> 橡皮擦工具组包括：橡皮擦工具、背景橡皮擦工具、魔术橡皮擦工具

PS 橡皮擦工具：图像擦除工具主要用了擦除多余的图像

- 橡皮擦工具：将像素更改为背景色或透明。使用橡皮擦工具在背景图层或锁定了透明像素的图层中进行擦除，则擦除的像素将变成背景色；如果在普通图层中进行擦除，则擦除的像素将变成透明
- 背景橡皮擦工具：在抹除背景的同时保留前景对象的边缘。背景橡皮擦工具是一种智能化的橡皮擦。设置好背景色后，使用该工具可以在抹除背景的同时保留前景对象的边缘（调小容差）
- 魔术橡皮擦工具：将所有相似的像素更改为透明。使用该工具在图像中单击时，可以将所有相似的像素更改为透明，效果等同于魔棒工具

ps 橡皮擦工具放大缩小快捷键：<kbd>]</kbd> 键和<kbd>[</kbd>键（可进行微调）

## 渐变工具组：G

> 渐变工具组：包括渐变工具、油漆桶工具、3D 材质拖放工具

- 渐变工具主要实现的功能是使颜色从一种到另一种的变化，达到两种或两种以上颜色混合的效果
- 油漆桶工具可以在图像中填充前景色或图案。这个工具在平时运用过程中被快捷方式替代，除了图案的填充

渐变工具选项栏功能详解：

- 渐变色条：显示出当前渐变的颜色。点击渐变颜色条会弹出渐变编辑器窗口；点击右侧的下拉按钮可以打开渐变拾色器窗口
- 渐变类型：目前有 5 种渐变类型，点击并选中想要渐变的类型
  - 线性渐变是以直线的方式创建从起点到终点的渐变
  - 径向渐变可以以圆形方式创建从起点到终点的渐变
  - 角度渐变可以创建围绕起点以逆时针扫描方式的渐变
  - 对称渐变可以使用对称均衡的线性渐变在起点的任意一侧创建渐变
  - 菱形渐变可以以菱形方式从起点向外产生渐变，终点定义菱形一个角
- 模式：设置应用渐变时所才用的混合模式
- 不透明度：用来设置渐变色的不透明度
- 反向：选中后可以得到和原来相反方向的颜色渐变结果
- 仿色：选中该选项时，渐变效果将会更加平滑。可以防止在打印渐变图像时出现条带状的结果，在电脑屏幕上并不能明显的看出来
- 透明区域：选中该选项时，可以创建从实色到透明色的渐变效果

ps 渐变编辑器：选中渐变工具后，单击选项栏中的渐变色条，即可打开渐变编辑器窗口，在该窗口中可以对渐变进行编辑

- 添加渐变色标：可以将光标移动到渐变色条的下方，光标变成小手形状时点击后即可添加一个色标
- 设置渐变颜色：渐变色条默认的渐变色是黑色到白色。双击渐变色条左端下方的起点色标，可弹出拾色器窗口，可以对该渐变的起点颜色进行设置
- 删除色标：点击选中要删除的色标，将其向下拖拽，或者按 Delete 键即可将其删除
- 调整颜色之间的过渡效果：可以左右来回拖拽渐变色条下方的黑色小菱形图标，找到合适的过渡点

## 模糊工具组：

## 减淡工具组：

## 钢笔工具组：P

> 钢笔工具组包括：钢笔工具、自由钢笔工具、弯度钢笔工具、添加锚点工具、删除锚点工具、转换点工具（显示和影藏手柄）

PS 钢笔工具是我们最常用的工具之一，其功能也是非常强大，可以用来抠图、绘画，其可以绘制出封闭或不封闭的矢量图形，称为路径，我们使用钢笔工具可以绘制出直线也可以绘制平滑的曲线，在线段上有一定的控制节点，我们可以通过节点调节自己需要的线条

- <kbd>Ctrl</kbd> + <kbd>Enter</kbd> 键直接将路径变成选区
- 添加锚点、删除锚点：直接点击即可
- 移动锚点：<kbd>Ctrl</kbd> + <kbd>指定的锚点</kbd> 或者选择路径选择工具组中的直接选择工具，进行选择单独的锚点
- 调整曲线角度：
  - 同时调整两个手柄方向：按住 <kbd>Alt</kbd>键，鼠标移动至锚点上，进而调整曲线的角度
  - 单独调整一个手柄方向：按住 <kbd>Alt</kbd>键，鼠标移动至手柄上，进而调整曲线的角度

## 横排文字工具组：T

> 横排文字工具组包括：横排文字工具、直排文字工具、直排文字蒙版工具、横排文字蒙版工具

<kbd>Ctrl</kbd> + <kbd>T</kbd>键可以调整文字大小

## 路径选择工具组：A

> 路径选择工具组包括：路径选择工具、直接选择工具

## 矩形工具组：U

> 矩形工具组包括：矩形工具、椭圆工具、三角形工具、多边形工具、直线工具、自定义形状工具

注意：矩形工具组制作出来的图像是矢量图形，若想把它变成一个像素图形，需要将其图层栅格化

如果选择矩形工具，想要画出正方形，按住 <kbd>Shift</kbd>键，即可画出正方形

- 并集：<kbd>Shift</kbd>键
- 差集：<kbd>Alt</kbd>键
- 交集：<kbd>Shift</kbd>键 + <kbd>Alt</kbd>键

## 抓手工具组：

## 缩放工具组：

## 前景色和背景色

- 填充前景色：<kbd>Alt</kbd> + <kbd>Delete</kbd>
- 填充背景色：<kbd>Ctrl</kbd> + <kbd>Delete</kbd>
- 切换前景色和背景色：<kbd>Shift</kbd> + <kbd>X</kbd>
- 填充：在当前图层或选区内填充颜色或图案或内容识别：<kbd>Shift</kbd> + <kbd>F5</kbd>
- 描边：在选区、路径或图层周围创建任何颜色的边框：<kbd>Alt</kbd> + <kbd>E</kbd> + <kbd>S</kbd>
